# English

Stub `.psc` headers for compiling with the official Papyrus compiler.
Do **not** ship these in the game `Data` folder — they are build-only.

Includes minimal SkyUI / MCM Helper / PapyrusUtil / Bestiary APIs used by this mod.

# Italiano

Header `.psc` di stub per compilare con il compilatore Papyrus ufficiale.
**Non** copiarli in `Data` del gioco: servono solo alla build.

Includono API minime SkyUI / MCM Helper / PapyrusUtil / Bestiary usate da questa mod.
